import 'package:lifesweettreatsordernotes/models/session.dart';

class SessionType {
  List<Session> preparations = List<Session>();
  List<Session> history = List<Session>();

  SessionType({this.preparations, this.history});
}